## form表单的样式, 规则如下:
```javascript
// 所有输入框或选择框都需要 label 标签来显示项目, 
// 所有输入框和 label 标签都放在一个 p 标签里
// 规定form表单的大小
// 规定输入框的宽度
// 输入框 float: right; 向右浮动
// label 标签 position: absolute; right: 240px; 绝对定位输入框的宽度
// 所有point 提示语句都用一个p单独占一行, 给定宽度高度
// point float: right; 向右浮动
```
